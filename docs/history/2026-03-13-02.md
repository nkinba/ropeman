# Sprint 2026-03-13-02 — Split Pane 버그 수정 + Export 정상화

## 완료 태스크

### U3 탭 분할 뷰

- HTML5 Drag and Drop API로 탭 드래그 → split 활성화
- SplitPane 컴포넌트: primary/secondary pane 독립 렌더링
- cross-pane 탭 드롭, 자동 split 해제
- `Ctrl+\` split 토글, `Ctrl+1/2` pane 포커스 전환

### ISS004 PNG/SVG Export 정상화

- Export 기능 수정

### Split Pane 버그 수정 (브라우저 테스트 완료)

- 비분할 모드 CodeViewer에 `filePath` prop 누락 수정
- `Ctrl+W`: focused pane의 active 탭을 닫도록 수정
- `Ctrl+Tab`: focused pane 내에서만 탭 순환
- `tabStore.closeTab`: secondary pane `activeTabId` 정리 로직 추가
- `SplitPane.handleSecondaryClose` 중복 로직 제거
- `activateTab` pane 인식, `moveTabToPane` activeTabId 정리
- 코드 탭 다중 오픈 (single-click=preview, double-click=pinned)

## 변경 파일

| 파일                                   | 변경 내용                                                        |
| -------------------------------------- | ---------------------------------------------------------------- |
| `src/lib/components/SplitPane.svelte`  | cross-pane 드롭, primaryActiveTab 필터, CodeViewer filePath 전달 |
| `src/lib/components/TabBar.svelte`     | 드래그 앤 드롭 지원                                              |
| `src/lib/components/CodeViewer.svelte` | `filePath` prop 추가                                             |
| `src/lib/stores/tabStore.svelte.ts`    | pane-aware activateTab, moveTabToPane, closeTab, openCodeTab     |
| `src/lib/stores/layoutStore.svelte.ts` | split 상태 관리 (isSplit, focusedPane, secondaryActiveTabId)     |
| `src/routes/+page.svelte`              | TabBar 반응성, drag-to-split, pane-aware 키보드 단축키           |

## 미해결 / 다음 스프린트

- **Issue #7**: N-pane 분할 뷰 지원 (3개 이상 pane)
- 시맨틱 트리 노드 클릭 시 다이어그램 pane 활성화 — AI 연결 후 테스트 필요
